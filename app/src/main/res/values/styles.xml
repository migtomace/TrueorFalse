<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Material">
        <!-- Customize your theme here. -->
    </style>

    <style name="tof" parent="AppTheme" >
        <item name="android:colorForeground">@color/foreground_material_dark</item>
    </style>

</resources>
